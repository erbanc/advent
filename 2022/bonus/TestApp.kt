class TestApp {

    fun main(args: Array) {
        var walls = listOf(2, 0, 4, 2, 0, 3, 2, 5, 0, 3, 0, 2)
        var currentWaterLevel = 0
        var leftWall = 0
        var rightWall = 0
        var wellVolume = 0
        var maximums = mutableListOf()
// Get maximums
        for (wall[ 1, walls.size] in walls) {
            if (wall > walls[$wall-1] && wall > walls[$wall+1]) prinln("test")
        }
    }
}
